{\rtf1\ansi\deff0 {\fonttbl {\f0 Times New Roman;}}\f0\fs24 {\qc\b\fs32 CONCEPTION DU SYSTEME DE GESTION DES EMPLOIS DU TEMPS\par}{\qc\fs24 \par}{\qc\b\fs28 Document de Conception Technique\par}{\qc\fs20 \par}{\qc Version: 1.0\par}{\qc Date: Octobre 2024\par}{\qc \par}{\qc Framework: Laravel 11\par}{\qc Base de Données: MySQL\par}\page {\b\fs28 CONCEPTION DU SYSTÈME DE GESTION DES EMPLOIS DU TEMPS\par}\par \par \par {\b\fs24 📋 INFORMATIONS GÉNÉRALES\par} \par {\b Titre du Projet} : Système de Gestion des Emplois du Temps\par {\b Version} : 1.0\par {\b Date} : Octobre 2024\par {\b Auteur} : Équipe Développement\par {\b Framework} : Laravel 11\par {\b Base de Données} : MySQL\par \par \par {\b\fs24 🎯 1. OBJECTIFS ET PORTÉE DU PROJET\par} \par {\b\fs22 1.1 Objectif Principal\par} Développer une application web complète permettant la gestion automatisée des emplois du temps dans les établissements d'enseignement supérieur, avec une prévention intelligente des conflits d'horaires et une interface utilisateur intuitive.\par \par {\b\fs22 1.2 Objectifs Spécifiques\par} • **Gestion hiérarchique** des structures académiques (Départements → Spécialités → Classes)\par • **Planification optimisée** des cours avec validation automatique des conflits\par • **Association dynamique** professeurs-matières selon les contextes pédagogiques\par • **Interface moderne** avec technologies web actuelles (Bootstrap 5, Select2, AJAX)\par • **Localisation française** complète avec messages d'erreur explicites\par \par {\b\fs22 1.3 Portée du Système\par} • **Utilisateurs cibles** : Administrateurs académiques, Secrétaires pédagogiques\par • **Établissements** : Universités, Écoles supérieures, Instituts de formation\par • **Périmètre fonctionnel** : Gestion complète des emplois du temps (création à impression)\par \par \par {\b\fs24 🏗️ 2. ARCHITECTURE GÉNÉRALE DU SYSTÈME\par} \par {\b\fs22 2.1 Architecture Technique\par} \par #### Stack Technologique\par Frontend :\par ├── HTML5/CSS3\par ├── Bootstrap 5 (Framework UI)\par ├── jQuery 3.6+ (Manipulation DOM)\par ├── Select2 (Sélections avancées)\par └── AJAX (Requêtes asynchrones)\par \par Backend :\par ├── PHP 8.1+\par ├── Laravel 11 (Framework MVC)\par ├── Eloquent ORM (Gestion BDD)\par └── Blade Templates (Moteur de vues)\par \par Base de Données :\par ├── MySQL 5.7+\par ├── Relations complexes\par ├── Contraintes d'intégrité\par └── Index optimisés\par \par #### Modèle MVC Laravel\par app/\par ├── Http/\par │   ├── Controllers/\par │   │   ├── EmploiTempsController.php\par │   │   ├── SubjectTeacherController.php\par │   │   ├── TeacherController.php\par │   │   └── ClasseController.php\par │   ├── Requests/\par │   │   └── EmploiTempsRequest.php\par │   └── Middleware/\par ├── Models/\par │   ├── EmploiTemps.php\par │   ├── EmploiHoraire.php\par │   ├── SubjectTeacher.php\par │   ├── Teacher.php\par │   ├── Subject.php\par │   ├── Classe.php\par │   ├── Speciality.php\par │   └── Horaire.php\par └── Services/\par ├── EmploiTempsService.php\par └── ValidationService.php\par \par {\b\fs22 2.2 Architecture Données\par} \par #### Hiérarchie Académique\par Anneescolaire (Année Scolaire)\par ├── Trimester (Trimestre)\par └── Departement (Département)\par └── Niveauformation (Niveau Formation)\par └── Speciality (Spécialité)\par ├── Classe (Classe)\par └── Subject (Matière)\par \par #### Relations Opérationnelles\par Teacher (Professeur) ←→ Subject (Matière)\par ↓ (via SubjectTeacher)\par Classe (Contexte pédagogique)\par ↓\par EmploiTemps (Emploi du temps)\par ↓ (relation many-to-many)\par Horaire (Créneaux horaires multiples)\par \par \par {\b\fs24 📊 3. CONCEPTION DE LA BASE DE DONNÉES\par} \par {\b\fs22 3.1 Modèle Conceptuel de Données (MCD)\par} \par #### Entités Principales\par \par {\b ANNEESCOLAIRE}\par • `id` : Identifiant unique\par • `annee_debut` : Année de début\par • `annee_fin` : Année de fin\par • `active` : Statut d'activation\par • `created_at`, `updated_at` : Timestamps\par \par {\b DEPARTEMENT}\par • `id` : Identifiant unique\par • `nom` : Nom du département\par • `description` : Description détaillée\par • `created_at`, `updated_at` : Timestamps\par \par {\b NIVEAUFORMATION}\par • `id` : Identifiant unique\par • `nom` : Nom du niveau (L1, L2, M1...)\par • `ordre` : Ordre de classement\par • `created_at`, `updated_at` : Timestamps\par \par {\b SPECIALITY}\par • `id` : Identifiant unique\par • `nom` : Nom de la spécialité\par • `departement_id` : Référence département\par • `niveauformation_id` : Référence niveau\par • `created_at`, `updated_at` : Timestamps\par \par {\b CLASSE}\par • `id` : Identifiant unique\par • `nom` : Nom de la classe\par • `speciality_id` : Référence spécialité\par • `niveau_pedagogique_id` : Référence niveau pédagogique\par • `created_at`, `updated_at` : Timestamps\par \par {\b TEACHER}\par • `id` : Identifiant unique\par • `name` : Nom complet\par • `email` : Email professionnel\par • `telephone` : Numéro de téléphone\par • `created_at`, `updated_at` : Timestamps\par \par {\b SUBJECT}\par • `id` : Identifiant unique\par • `name` : Nom de la matière\par • `code` : Code matière\par • `speciality_id` : Référence spécialité\par • `created_at`, `updated_at` : Timestamps\par \par {\b TRIMESTER}\par • `id` : Identifiant unique\par • `name` : Nom du trimestre\par • `annee_id` : Référence année scolaire\par • `date_debut` : Date de début\par • `date_fin` : Date de fin\par • `created_at`, `updated_at` : Timestamps\par \par {\b HORAIRE}\par • `id` : Identifiant unique\par • `libelle_fr` : Libellé français\par • `libelle_ar` : Libellé arabe\par • `start_time` : Heure de début\par • `end_time` : Heure de fin\par • `ordre` : Ordre de classement\par • `created_at`, `updated_at` : Timestamps\par \par {\b JOUR}\par • `id` : Identifiant unique\par • `nom` : Nom du jour\par • `ordre` : Ordre dans la semaine\par • `created_at`, `updated_at` : Timestamps\par \par {\b SALLE_DE_CLASSE}\par • `id` : Identifiant unique\par • `nom` : Nom de la salle\par • `capacite` : Capacité d'accueil\par • `equipement` : Description équipements\par • `created_at`, `updated_at` : Timestamps\par \par {\b\fs22 3.2 Tables de Liaison et Associations\par} \par {\b SUBJECT_TEACHER} (Association Professeur-Matière)\par • `id` : Identifiant unique\par • `teacher_id` : Référence professeur\par • `subject_id` : Référence matière\par • `class_id` : Référence classe (CONTEXTE)\par • `trimester_id` : Référence trimestre\par • `annee_id` : Référence année scolaire\par • `created_at`, `updated_at` : Timestamps\par \par {\b EMPLOIS_TEMPS} (Emploi du Temps Principal)\par • `id` : Identifiant unique\par • `class_id` : Référence classe\par • `subject_id` : Référence matière\par • `teacher_id` : Référence professeur\par • `trimester_id` : Référence trimestre\par • `annee_id` : Référence année scolaire\par • `jour_id` : Référence jour\par • `salle_de_classe_id` : Référence salle (optionnel)\par • `created_at`, `updated_at` : Timestamps\par \par {\b EMPLOI_HORAIRE} (Horaires Multiples)\par • `id` : Identifiant unique\par • `emploi_temps_id` : Référence emploi du temps\par • `horaire_id` : Référence horaire\par • `created_at`, `updated_at` : Timestamps\par \par {\b\fs22 3.3 Contraintes d'Intégrité\par} \par #### Clés Étrangères\par ALTER TABLE specialities ADD CONSTRAINT fk_speciality_departement\par FOREIGN KEY (departement_id) REFERENCES departements(id) ON DELETE CASCADE;\par \par ALTER TABLE classes ADD CONSTRAINT fk_classe_speciality\par FOREIGN KEY (speciality_id) REFERENCES specialities(id) ON DELETE CASCADE;\par \par ALTER TABLE subject_teacher ADD CONSTRAINT fk_st_teacher\par FOREIGN KEY (teacher_id) REFERENCES teachers(id) ON DELETE CASCADE;\par \par ALTER TABLE emplois_temps ADD CONSTRAINT fk_emploi_classe\par FOREIGN KEY (class_id) REFERENCES classes(id) ON DELETE CASCADE;\par \par ALTER TABLE emploi_horaire ADD CONSTRAINT fk_emploi_horaire\par FOREIGN KEY (emploi_temps_id) REFERENCES emplois_temps(id) ON DELETE CASCADE;\par \par #### Index de Performance\par CREATE INDEX idx_subject_teacher_lookup ON subject_teacher\par (class_id, trimester_id, annee_id);\par \par CREATE INDEX idx_emploi_temps_search ON emplois_temps\par (class_id, trimester_id, jour_id);\par \par CREATE INDEX idx_emploi_horaire_emploi ON emploi_horaire\par (emploi_temps_id);\par \par CREATE INDEX idx_conflict_detection ON emplois_temps\par (teacher_id, jour_id, trimester_id);\par \par #### Contraintes Uniques\par ALTER TABLE subject_teacher ADD CONSTRAINT uk_subject_teacher_context\par UNIQUE (teacher_id, subject_id, class_id, trimester_id);\par \par ALTER TABLE emploi_horaire ADD CONSTRAINT uk_emploi_horaire\par UNIQUE (emploi_temps_id, horaire_id);\par \par \par {\b\fs24 🔧 4. CONCEPTION FONCTIONNELLE\par} \par {\b\fs22 4.1 Diagrammes de Cas d'Usage\par} \par #### Acteurs Principaux\par • **Administrateur Académique** : Gestion complète du système\par • **Secrétaire Pédagogique** : Saisie et modification des emplois du temps\par • **Professeur** : Consultation de ses emplois (futur)\par • **Étudiant** : Consultation emplois de classe (futur)\par \par #### Cas d'Usage Principaux\par \par {\b CU01 : Configurer Structure Académique}\par • Acteur : Administrateur\par • Description : Créer départements, spécialités, classes\par • Préconditions : Droits d'administration\par • Postconditions : Structure créée et validée\par \par {\b CU02 : Gérer Professeurs et Matières}\par • Acteur : Administrateur\par • Description : Ajouter professeurs, matières, associations\par • Préconditions : Structure académique existante\par • Postconditions : Personnel et matières opérationnels\par \par {\b CU03 : Créer Emploi du Temps}\par • Acteur : Secrétaire Pédagogique\par • Description : Planifier cours avec validation automatique\par • Préconditions : Configuration complète\par • Postconditions : Emploi du temps validé et sauvegardé\par \par {\b CU04 : Valider Conflits d'Horaires}\par • Acteur : Système\par • Description : Vérification automatique des conflits\par • Préconditions : Données emploi saisies\par • Postconditions : Validation réussie ou erreurs détectées\par \par {\b\fs22 4.2 Workflow de Création d'Emploi du Temps\par} \par 1. Sélection Classe\par ↓\par 2. Auto-chargement Trimestre Actif\par ↓\par 3. Filtrage Professeurs Disponibles\par (via subject_teacher + contexte classe)\par ↓\par 4. Sélection Professeur\par ↓\par 5. Auto-filtrage Matières Autorisées\par (selon associations professeur-classe)\par ↓\par 6. Sélection Matière, Jour, Horaires\par ↓\par 7. Validation Conflits Multi-niveaux\par • Professeur disponible ?\par • Salle libre ?\par • Classe libre ?\par ↓\par 8. Sauvegarde ou Correction Erreurs\par \par {\b\fs22 4.3 Règles de Gestion Métier\par} \par #### Règles de Validation des Conflits\par \par {\b RG01 : Unicité Professeur-Horaire}\par • Un professeur ne peut enseigner qu'une seule matière par créneau horaire\par • Vérification : `(teacher_id, jour_id, horaire_id, trimester_id) = UNIQUE`\par \par {\b RG02 : Unicité Salle-Horaire}\par • Une salle ne peut accueillir qu'un seul cours par créneau\par • Vérification : `(salle_id, jour_id, horaire_id, trimester_id) = UNIQUE`\par \par {\b RG03 : Unicité Classe-Horaire}\par • Une classe ne peut avoir qu'un cours par créneau\par • Vérification : `(class_id, jour_id, horaire_id, trimester_id) = UNIQUE`\par \par {\b RG04 : Cohérence Pédagogique}\par • Le professeur doit être autorisé à enseigner la matière pour cette classe\par • Vérification : Existence dans `subject_teacher` avec contexte complet\par \par {\b RG05 : Contraintes Temporelles}\par • Les emplois du temps sont créés pour le trimestre actif\par • Horaires multiples possibles pour un même cours\par • Respect de l'ordre chronologique des créneaux\par \par #### Règles de Configuration\par \par {\b RC01 : Ordre de Configuration}\par 1. Années scolaires → Activer année courante\par 2. Structure académique (Départements → Spécialités → Classes)\par 3. Personnel (Professeurs, Matières)\par 4. Associations (Professeur-Matière par classe)\par 5. Configuration temporelle (Trimestres, Jours, Horaires)\par 6. Création emplois du temps\par \par {\b RC02 : Validation des Associations}\par • Association professeur-matière obligatoire avant création emploi\par • Contexte classe + trimestre requis pour association\par • Validation des compétences pédagogiques\par \par \par {\b\fs24 💻 5. CONCEPTION DE L'INTERFACE UTILISATEUR\par} \par {\b\fs22 5.1 Principes de Design\par} \par #### Ergonomie et Accessibilité\par • **Design responsive** : Bootstrap 5 pour adaptation multi-écrans\par • **Interface intuitive** : Navigation claire et logique\par • **Feedback visuel** : Messages d'erreur et de confirmation explicites\par • **Localisation** : Interface entièrement en français\par \par #### Standards UX/UI\par • **Cohérence visuelle** : Charte graphique uniforme\par • **Temps de réponse** : Chargement AJAX pour fluidité\par • **Validation temps réel** : Retour immédiat sur les erreurs\par • **Assistance contextuelle** : Messages d'aide intégrés\par \par {\b\fs22 5.2 Architecture des Vues\par} \par #### Structure des Templates Blade\par resources/views/\par ├── layouts/\par │   ├── masters/\par │   │   └── master.blade.php          # Template principal\par │   └── components/\par │       ├── sidebar.blade.php         # Navigation latérale\par │       └── header.blade.php          # En-tête\par ├── admin/\par │   ├── emplois/\par │   │   ├── index.blade.php           # Liste emplois\par │   │   ├── create.blade.php          # Formulaire création\par │   │   └── edit.blade.php            # Formulaire modification\par │   ├── sct_emplois_temps/\par │   │   └── pdf/\par │   │       └── classe_emplois_pdf.blade.php  # Export PDF\par │   └── partials/\par │       ├── form_emploi.blade.php     # Formulaire dynamique\par │       └── validation_errors.blade.php  # Affichage erreurs\par └── components/\par ├── select2.blade.php             # Composant Select2\par └── datatable.blade.php           # Tableaux dynamiques\par \par #### Composants JavaScript\par // Fonctionnalités principales\par class EmploiTempsManager {\par // Gestion formulaire dynamique\par addRow()\par removeRow()\par \par // Chargement AJAX\par loadTeachers(classId, trimesterId)\par loadSubjects(teacherId, classId)\par \par // Validation temps réel\par validateConflicts()\par displayErrors()\par \par // Interface Select2\par initializeSelect2()\par updateOptions()\par }\par \par {\b\fs22 5.3 Formulaires et Interactions\par} \par #### Formulaire Principal de Création\par <form id="emploi-form" action="{{ route('web.emplois.store') }}" method="POST">\par <!-- Sélection classe (contexte principal) -->\par <select name="class_id" id="class_id" class="form-control" required>\par <option value="">Sélectionner une classe</option>\par @foreach($classes as $classe)\par <option value="{{ $classe->id }}">{{ $classe->nom }}</option>\par @endforeach\par </select>\par \par <!-- Trimestre (auto-sélectionné selon année active) -->\par <select name="trimester_id" id="trimester_id" class="form-control" required>\par <!-- Options chargées dynamiquement -->\par </select>\par \par <!-- Lignes dynamiques pour chaque cours -->\par <div id="cours-container">\par <div class="cours-row">\par <!-- Professeur (filtré par classe) -->\par <select name="teacher_id[]" class="teacher-select" required>\par <!-- Chargé via AJAX selon classe -->\par </select>\par \par <!-- Matière (filtrée par professeur + classe) -->\par <select name="subject_id[]" class="subject-select" required>\par <!-- Chargé via AJAX selon professeur/classe -->\par </select>\par \par <!-- Jour -->\par <select name="jour_id[]" class="form-control" required>\par @foreach($jours as $jour)\par <option value="{{ $jour->id }}">{{ $jour->nom }}</option>\par @endforeach\par </select>\par \par <!-- Horaires multiples (Select2) -->\par <select name="horaire_id[][]" class="horaire-select" multiple required>\par @foreach($horaires as $horaire)\par <option value="{{ $horaire->id }}">{{ $horaire->libelle_fr }}</option>\par @endforeach\par </select>\par \par <!-- Salle (optionnel) -->\par <select name="salle_de_classe_id[]" class="form-control">\par <option value="">Sélectionner une salle (optionnel)</option>\par @foreach($salles as $salle)\par <option value="{{ $salle->id }}">{{ $salle->nom }}</option>\par @endforeach\par </select>\par </div>\par </div>\par \par <!-- Boutons d'action -->\par <button type="button" id="add-cours" class="btn btn-secondary">\par <i class="fa fa-plus"></i> Ajouter un cours\par </button>\par <button type="submit" class="btn btn-primary">\par <i class="fa fa-save"></i> Enregistrer l'emploi du temps\par </button>\par </form>\par \par #### Interactions AJAX\par // Chargement professeurs selon classe\par $('#class_id').on('change', function() {\par let classId = $(this).val();\par let trimesterId = $('#trimester_id').val();\par \par if (classId && trimesterId) {\par $.ajax({\par url: '/admin/emplois/teachers',\par data: { class_id: classId, trimester_id: trimesterId },\par success: function(data) {\par $('.teacher-select').html(data.html);\par $('.subject-select').html('<option value="">Sélectionner un professeur d\'abord</option>');\par }\par });\par }\par });\par \par // Chargement matières selon professeur\par $(document).on('change', '.teacher-select', function() {\par let teacherId = $(this).val();\par let classId = $('#class_id').val();\par let subjectSelect = $(this).closest('.cours-row').find('.subject-select');\par \par if (teacherId && classId) {\par $.ajax({\par url: '/admin/emplois/subjects',\par data: { teacher_id: teacherId, class_id: classId },\par success: function(data) {\par subjectSelect.html(data.html);\par }\par });\par }\par });\par \par \par {\b\fs24 🔒 6. SÉCURITÉ ET VALIDATION\par} \par {\b\fs22 6.1 Validation des Données\par} \par #### Validation Backend (Laravel)\par // EmploiTempsRequest - Règles de validation\par public function rules()\par {\par return [\par 'class_id' => 'required|exists:classes,id',\par 'trimester_id' => 'required|exists:trimesters,id',\par 'teacher_id.*' => 'required|exists:teachers,id',\par 'subject_id.*' => 'required|exists:subjects,id',\par 'jour_id.*' => 'required|exists:jours,id',\par 'horaire_id.{\i .}' => 'required|exists:sct_ref_horaires,id',\par 'salle_de_classe_id.*' => 'nullable|exists:salle_de_classes,id',\par ];\par }\par \par // Messages d'erreur personnalisés\par public function messages()\par {\par return [\par 'class_id.required' => 'La classe est obligatoire',\par 'teacher_id.*.exists' => 'Professeur sélectionné invalide',\par 'horaire_id.{\i .}.required' => 'Au moins un horaire doit être sélectionné',\par // ...\par ];\par }\par \par #### Validation Métier Complexe\par // EmploiTempsService - Validation des conflits\par public function validateConflicts($data)\par {\par $errors = [];\par \par foreach ($data['teacher_id'] as $index => $teacherId) {\par $jourId = $data['jour_id'][$index];\par $horaireIds = $data['horaire_id'][$index];\par \par foreach ($horaireIds as $horaireId) {\par // Conflit professeur\par $conflictProf = $this->checkTeacherConflict($teacherId, $jourId, $horaireId, $data['trimester_id']);\par if ($conflictProf) {\par $errors[] = "Conflit professeur détecté : {$conflictProf->message}";\par }\par \par // Conflit salle\par if ($data['salle_de_classe_id'][$index]) {\par $conflictSalle = $this->checkSalleConflict($data['salle_de_classe_id'][$index], $jourId, $horaireId, $data['trimester_id']);\par if ($conflictSalle) {\par $errors[] = "Conflit salle détecté : {$conflictSalle->message}";\par }\par }\par \par // Conflit classe\par $conflictClasse = $this->checkClasseConflict($data['class_id'], $jourId, $horaireId, $data['trimester_id']);\par if ($conflictClasse) {\par $errors[] = "Conflit classe détecté : {$conflictClasse->message}";\par }\par }\par \par // Validation association professeur-matière\par $validAssociation = $this->checkSubjectTeacherAssociation($teacherId, $data['subject_id'][$index], $data['class_id'], $data['trimester_id']);\par if (!$validAssociation) {\par $errors[] = "Association professeur-matière non autorisée pour cette classe";\par }\par }\par \par return $errors;\par }\par \par {\b\fs22 6.2 Sécurité des Accès\par} \par #### Middleware d'Authentification\par // Routes protégées\par Route::middleware(['auth', 'admin'])->prefix('admin')->name('web.')->group(function () {\par Route::resource('emplois', EmploiTempsController::class);\par Route::get('emplois/teachers', [EmploiTempsController::class, 'getTeachers']);\par Route::get('emplois/subjects', [EmploiTempsController::class, 'getSubjects']);\par });\par \par #### Protection CSRF\par <!-- Formulaires protégés -->\par <form method="POST" action="{{ route('web.emplois.store') }}">\par @csrf\par <!-- Contenu formulaire -->\par </form>\par \par #### Validation des Entrées\par // Sanitization des données\par public function store(EmploiTempsRequest $request)\par {\par // Données déjà validées par le Request\par $validatedData = $request->validated();\par \par // Validation métier supplémentaire\par $conflicts = $this->emploiService->validateConflicts($validatedData);\par \par if (!empty($conflicts)) {\par return back()->withErrors($conflicts)->withInput();\par }\par \par // Sauvegarde sécurisée\par DB::transaction(function () use ($validatedData) {\par $this->emploiService->createEmploi($validatedData);\par });\par \par return redirect()->route('web.emplois.index')->with('success', 'Emploi du temps créé avec succès');\par }\par \par \par {\b\fs24 📈 7. PERFORMANCES ET OPTIMISATION\par} \par {\b\fs22 7.1 Optimisation Base de Données\par} \par #### Stratégie d'Indexation\par -- Index composites pour requêtes fréquentes\par CREATE INDEX idx_emploi_context ON emplois_temps\par (class_id, trimester_id, annee_id);\par \par CREATE INDEX idx_subject_teacher_context ON subject_teacher\par (class_id, trimester_id, teacher_id, subject_id);\par \par CREATE INDEX idx_conflict_detection ON emplois_temps\par (teacher_id, jour_id, trimester_id);\par \par -- Index pour recherches de conflits\par CREATE INDEX idx_salle_planning ON emplois_temps\par (salle_de_classe_id, jour_id, trimester_id);\par \par #### Requêtes Optimisées\par // Chargement optimisé avec relations\par public function getEmploisWithDetails($classId, $trimesterId)\par {\par return EmploiTemps::with([\par 'classe:id,nom',\par 'subject:id,name',\par 'teacher:id,name',\par 'trimester:id,name',\par 'jour:id,nom,ordre',\par 'horairess:id,libelle_fr,start_time,end_time',\par 'salleDeClasse:id,nom'\par ])\par ->where('class_id', $classId)\par ->where('trimester_id', $trimesterId)\par ->orderBy('jour_id')\par ->get();\par }\par \par // Validation de conflits optimisée\par public function checkTeacherConflict($teacherId, $jourId, $horaireId, $trimesterId)\par {\par return EmploiTemps::whereHas('horairess', function($query) use ($horaireId) {\par $query->where('sct_ref_horaires.id', $horaireId);\par })\par ->where('teacher_id', $teacherId)\par ->where('jour_id', $jourId)\par ->where('trimester_id', $trimesterId)\par ->with(['subject:id,name', 'classe:id,nom'])\par ->first();\par }\par \par {\b\fs22 7.2 Optimisation Frontend\par} \par #### Chargement AJAX Optimisé\par // Cache des données fréquemment utilisées\par class DataCache {\par constructor() {\par this.teachers = new Map();\par this.subjects = new Map();\par }\par \par getTeachers(classId, trimesterId) {\par const key = ${classId}-${trimesterId};\par if (this.teachers.has(key)) {\par return Promise.resolve(this.teachers.get(key));\par }\par \par return $.ajax({\par url: '/admin/emplois/teachers',\par data: { class_id: classId, trimester_id: trimesterId }\par }).then(data => {\par this.teachers.set(key, data);\par return data;\par });\par }\par }\par \par // Debounce pour les requêtes fréquentes\par const debouncedValidation = debounce(validateForm, 300);\par \par function debounce(func, wait) {\par let timeout;\par return function executedFunction(...args) {\par const later = () => {\par clearTimeout(timeout);\par func(...args);\par };\par clearTimeout(timeout);\par timeout = setTimeout(later, wait);\par };\par }\par \par #### Optimisation Select2\par // Configuration Select2 optimisée\par $('.horaire-select').select2({\par theme: 'bootstrap-5',\par placeholder: 'Sélectionner les horaires',\par allowClear: true,\par closeOnSelect: false,\par templateResult: formatHoraireOption,\par templateSelection: formatHoraireSelection\par });\par \par function formatHoraireOption(horaire) {\par if (!horaire.id) return horaire.text;\par \par return $(`\par <span class="horaire-option">\par <strong>${horaire.text}</strong>\par <small class="text-muted">(${horaire.element.dataset.time})</small>\par </span>\par `);\par }\par \par \par {\b\fs24 🧪 8. TESTS ET QUALITÉ\par} \par {\b\fs22 8.1 Stratégie de Tests\par} \par #### Tests Unitaires (PHPUnit)\par // Tests des modèles\par class EmploiTempsTest extends TestCase\par {\par use RefreshDatabase;\par \par public function test_emploi_temps_creation()\par {\par $emploi = EmploiTemps::factory()->create();\par \par $this->assertDatabaseHas('emplois_temps', [\par 'id' => $emploi->id,\par 'class_id' => $emploi->class_id,\par 'teacher_id' => $emploi->teacher_id\par ]);\par }\par \par public function test_teacher_conflict_validation()\par {\par // Créer un emploi du temps existant\par $existingEmploi = EmploiTemps::factory()->create([\par 'teacher_id' => 1,\par 'jour_id' => 1,\par 'trimester_id' => 1\par ]);\par \par // Ajouter un horaire\par $existingEmploi->horairess()->attach(1);\par \par // Tenter de créer un conflit\par $service = new EmploiTempsService();\par $conflicts = $service->checkTeacherConflict(1, 1, 1, 1);\par \par $this->assertNotNull($conflicts);\par }\par }\par \par // Tests des contrôleurs\par class EmploiTempsControllerTest extends TestCase\par {\par public function test_create_emploi_with_valid_data()\par {\par $response = $this->post(route('web.emplois.store'), [\par 'class_id' => 1,\par 'trimester_id' => 1,\par 'teacher_id' => [1],\par 'subject_id' => [1],\par 'jour_id' => [1],\par 'horaire_id' => [[1, 2]]\par ]);\par \par $response->assertRedirect(route('web.emplois.index'));\par $response->assertSessionHas('success');\par }\par \par public function test_create_emploi_with_conflict()\par {\par // Créer un emploi existant\par EmploiTemps::factory()->create()->horairess()->attach(1);\par \par $response = $this->post(route('web.emplois.store'), [\par // Même données (conflit)\par ]);\par \par $response->assertSessionHasErrors();\par }\par }\par \par #### Tests d'Intégration\par // Test du workflow complet\par class EmploiTempsWorkflowTest extends TestCase\par {\par public function test_complete_emploi_creation_workflow()\par {\par // 1. Créer structure académique\par $departement = Departement::factory()->create();\par $speciality = Speciality::factory()->create(['departement_id' => $departement->id]);\par $classe = Classe::factory()->create(['speciality_id' => $speciality->id]);\par \par // 2. Créer professeurs et matières\par $teacher = Teacher::factory()->create();\par $subject = Subject::factory()->create(['speciality_id' => $speciality->id]);\par \par // 3. Créer association professeur-matière\par SubjectTeacher::factory()->create([\par 'teacher_id' => $teacher->id,\par 'subject_id' => $subject->id,\par 'class_id' => $classe->id\par ]);\par \par // 4. Test création emploi du temps\par $response = $this->post(route('web.emplois.store'), [\par 'class_id' => $classe->id,\par 'teacher_id' => [$teacher->id],\par 'subject_id' => [$subject->id],\par // ... autres données\par ]);\par \par // 5. Vérifications\par $response->assertSuccessful();\par $this->assertDatabaseHas('emplois_temps', ['class_id' => $classe->id]);\par $this->assertDatabaseHas('emploi_horaire', ['emploi_temps_id' => 1]);\par }\par }\par \par {\b\fs22 8.2 Tests Frontend (JavaScript)\par} \par #### Tests Unitaires JavaScript\par // Tests des fonctions utilitaires\par describe('EmploiTempsManager', () => {\par let manager;\par \par beforeEach(() => {\par manager = new EmploiTempsManager();\par document.body.innerHTML = '<form id="emploi-form"></form>';\par });\par \par test('should add new course row', () => {\par manager.addRow();\par const rows = document.querySelectorAll('.cours-row');\par expect(rows.length).toBe(1);\par });\par \par test('should validate conflicts correctly', async () => {\par const formData = {\par teacher_id: [1],\par jour_id: [1],\par horaire_id: [[1, 2]]\par };\par \par const conflicts = await manager.validateConflicts(formData);\par expect(conflicts).toBeDefined();\par });\par });\par \par // Tests d'intégration AJAX\par describe('AJAX Integration', () => {\par test('should load teachers for selected class', async () => {\par const mockResponse = {\par success: true,\par html: '<option value="1">Prof 1</option>'\par };\par \par jest.spyOn(global, 'fetch').mockResolvedValue({\par ok: true,\par json: () => Promise.resolve(mockResponse)\par });\par \par const result = await loadTeachers(1, 1);\par expect(result.success).toBe(true);\par });\par });\par \par {\b\fs22 8.3 Tests de Performance\par} \par #### Tests de Charge\par // Test de performance des requêtes\par class PerformanceTest extends TestCase\par {\par public function test_emploi_loading_performance()\par {\par // Créer données de test volumineuses\par Emploi::factory(1000)->create();\par \par $startTime = microtime(true);\par \par $emplois = EmploiTemps::with(['classe', 'teacher', 'subject', 'horairess'])\par ->limit(100)\par ->get();\par \par $executionTime = microtime(true) - $startTime;\par \par $this->assertLessThan(0.1, $executionTime); // Moins de 100ms\par $this->assertCount(100, $emplois);\par }\par \par public function test_conflict_detection_performance()\par {\par $service = new EmploiTempsService();\par \par $startTime = microtime(true);\par \par $conflicts = $service->checkAllConflicts(1, 1, 1, 1);\par \par $executionTime = microtime(true) - $startTime;\par \par $this->assertLessThan(0.05, $executionTime); // Moins de 50ms\par }\par }\par \par \par {\b\fs24 📦 9. DÉPLOIEMENT ET MAINTENANCE\par} \par {\b\fs22 9.1 Environnements\par} \par #### Configuration des Environnements\par {\b\fs28 Développement\par}\par APP_ENV=local\par APP_DEBUG=true\par DB_CONNECTION=mysql\par DB_HOST=127.0.0.1\par DB_DATABASE=gestion_emplois_dev\par \par {\b\fs28 Test\par}\par APP_ENV=testing\par APP_DEBUG=true\par DB_CONNECTION=sqlite\par DB_DATABASE=:memory:\par \par {\b\fs28 Production\par}\par APP_ENV=production\par APP_DEBUG=false\par DB_CONNECTION=mysql\par DB_HOST=production-server\par DB_DATABASE=gestion_emplois_prod\par \par #### Scripts de Déploiement\par #!/bin/bash\par {\b\fs28 deploy.sh\par}\par \par echo "🚀 Déploiement Système Gestion Emplois du Temps"\par \par {\b\fs28 Mise à jour code\par}\par git pull origin main\par \par {\b\fs28 Dependencies\par}\par composer install --no-dev --optimize-autoloader\par npm ci --only=production\par \par {\b\fs28 Base de données\par}\par php artisan migrate --force\par \par {\b\fs28 Cache et optimisations\par}\par php artisan config:cache\par php artisan route:cache\par php artisan view:cache\par php artisan optimize\par \par {\b\fs28 Assets\par}\par npm run build\par \par {\b\fs28 Permissions\par}\par chown -R www-data:www-data storage bootstrap/cache\par chmod -R 755 storage bootstrap/cache\par \par echo "✅ Déploiement terminé avec succès"\par \par {\b\fs22 9.2 Monitoring et Logs\par} \par #### Configuration Logs Laravel\par // config/logging.php\par 'channels' => [\par 'emplois' => [\par 'driver' => 'daily',\par 'path' => storage_path('logs/emplois.log'),\par 'level' => 'info',\par 'days' => 14,\par ],\par \par 'conflicts' => [\par 'driver' => 'daily',\par 'path' => storage_path('logs/conflicts.log'),\par 'level' => 'warning',\par 'days' => 30,\par ],\par ];\par \par #### Logging Applicatif\par // EmploiTempsController\par public function store(Request $request)\par {\par Log::channel('emplois')->info('Création emploi du temps', [\par 'user_id' => auth()->id(),\par 'class_id' => $request->class_id,\par 'data' => $request->all()\par ]);\par \par try {\par $emploi = $this->emploiService->createEmploi($request->validated());\par \par Log::channel('emplois')->info('Emploi du temps créé', [\par 'emploi_id' => $emploi->id,\par 'success' => true\par ]);\par \par } catch (ConflictException $e) {\par Log::channel('conflicts')->warning('Conflit détecté', [\par 'error' => $e->getMessage(),\par 'data' => $request->all()\par ]);\par \par throw $e;\par }\par }\par \par {\b\fs22 9.3 Maintenance et Évolutions\par} \par #### Tâches de Maintenance\par // app/Console/Commands/MaintenanceEmplois.php\par class MaintenanceEmplois extends Command\par {\par protected $signature = 'emplois:maintenance';\par protected $description = 'Maintenance système emplois du temps';\par \par public function handle()\par {\par $this->info('🔧 Début maintenance emplois du temps');\par \par // Nettoyage emplois anciens trimestres\par $this->cleanOldEmplois();\par \par // Vérification intégrité données\par $this->checkDataIntegrity();\par \par // Optimisation base de données\par $this->optimizeDatabase();\par \par $this->info('✅ Maintenance terminée');\par }\par \par private function cleanOldEmplois()\par {\par $deleted = EmploiTemps::whereHas('trimester', function($query) {\par $query->where('date_fin', '<', now()->subYear());\par })->delete();\par \par $this->info("🗑️  {$deleted} anciens emplois supprimés");\par }\par \par private function checkDataIntegrity()\par {\par $orphans = EmploiTemps::whereNotExists(function($query) {\par $query->select(DB::raw(1))\par ->from('subject_teacher')\par ->whereRaw('subject_teacher.teacher_id = emplois_temps.teacher_id')\par ->whereRaw('subject_teacher.subject_id = emplois_temps.subject_id')\par ->whereRaw('subject_teacher.class_id = emplois_temps.class_id');\par })->count();\par \par if ($orphans > 0) {\par $this->warn("⚠️  {$orphans} emplois sans association professeur-matière valide");\par }\par }\par }\par \par #### Évolutions Prévues\par {\b\fs28 Roadmap Évolutions\par}\par \par {\b\fs24 Version 1.1 (Q1 2025)\par} • [ ] Gestion des salles avec contraintes d'équipement\par • [ ] Export emplois format Excel\par • [ ] Notifications par email aux professeurs\par • [ ] Interface mobile native\par \par {\b\fs24 Version 1.2 (Q2 2025)\par} • [ ] Planification automatique par algorithme\par • [ ] Gestion des remplacements de professeurs\par • [ ] Statistiques et rapports avancés\par • [ ] API REST pour intégrations externes\par \par {\b\fs24 Version 2.0 (Q3 2025)\par} • [ ] Multi-établissements\par • [ ] Gestion des évaluations et examens\par • [ ] Module de présence et retards\par • [ ] Interface étudiants avec authentification\par \par \par {\b\fs24 📚 10. ANNEXES\par} \par {\b\fs22 10.1 Glossaire Technique\par} \par {\b AJAX} : Asynchronous JavaScript And XML - Technique permettant des requêtes asynchrones\par {\b Blade} : Moteur de templates de Laravel\par {\b Bootstrap} : Framework CSS pour interfaces responsives\par {\b Eloquent} : ORM (Object-Relational Mapping) de Laravel\par {\b Laravel} : Framework PHP MVC\par {\b MCD} : Modèle Conceptuel de Données\par {\b MVC} : Model-View-Controller (architecture logicielle)\par {\b ORM} : Object-Relational Mapping\par {\b Select2} : Plugin jQuery pour sélections avancées\par \par {\b\fs22 10.2 Références Techniques\par} \par #### Documentation Laravel\par • [Laravel 11 Documentation](https://laravel.com/docs/11.x)\par • [Eloquent ORM](https://laravel.com/docs/11.x/eloquent)\par • [Blade Templates](https://laravel.com/docs/11.x/blade)\par • [Validation](https://laravel.com/docs/11.x/validation)\par \par #### Bibliothèques Frontend\par • [Bootstrap 5](https://getbootstrap.com/docs/5.3/)\par • [Select2](https://select2.org/)\par • [jQuery](https://api.jquery.com/)\par \par #### Outils de Développement\par • [PHPUnit Testing](https://phpunit.de/documentation.html)\par • [Laravel Sail](https://laravel.com/docs/11.x/sail)\par • [Vite Asset Bundling](https://laravel.com/docs/11.x/vite)\par \par {\b\fs22 10.3 Diagrammes UML\par} \par #### Diagramme de Classes Principal\par @startuml\par class EmploiTemps {\par +id: BigInt\par +class_id: BigInt\par +subject_id: BigInt\par +teacher_id: BigInt\par +trimester_id: BigInt\par +jour_id: BigInt\par +salle_de_classe_id: BigInt\par +created_at: Timestamp\par +updated_at: Timestamp\par \par +horairess(): BelongsToMany\par +classe(): BelongsTo\par +subject(): BelongsTo\par +teacher(): BelongsTo\par }\par \par class EmploiHoraire {\par +id: BigInt\par +emploi_temps_id: BigInt\par +horaire_id: BigInt\par \par +emploi(): BelongsTo\par +horaire(): BelongsTo\par }\par \par class SubjectTeacher {\par +id: BigInt\par +teacher_id: BigInt\par +subject_id: BigInt\par +class_id: BigInt\par +trimester_id: BigInt\par +annee_id: BigInt\par \par +teacher(): BelongsTo\par +subject(): BelongsTo\par +classe(): BelongsTo\par }\par \par EmploiTemps ||--o{ EmploiHoraire\par EmploiTemps }o--|| SubjectTeacher\par @enduml\par \par #### Diagramme de Séquence - Création Emploi\par @startuml\par actor Utilisateur\par participant "Interface Web" as UI\par participant "EmploiTempsController" as Controller\par participant "EmploiTempsService" as Service\par participant "Base de Données" as DB\par \par Utilisateur -> UI: Sélectionne classe\par UI -> Controller: GET /emplois/teachers?class_id=1\par Controller -> DB: Récupère professeurs autorisés\par DB -> Controller: Liste professeurs\par Controller -> UI: JSON professeurs\par \par Utilisateur -> UI: Sélectionne professeur\par UI -> Controller: GET /emplois/subjects?teacher_id=1&class_id=1\par Controller -> DB: Récupère matières autorisées\par DB -> Controller: Liste matières\par Controller -> UI: JSON matières\par \par Utilisateur -> UI: Remplit formulaire complet\par UI -> Controller: POST /emplois (données formulaire)\par Controller -> Service: validateConflicts(data)\par Service -> DB: Vérifie conflits existants\par DB -> Service: Résultats requêtes\par \par alt Aucun conflit\par Service -> Controller: Validation OK\par Controller -> DB: Crée emploi + horaires\par DB -> Controller: Confirmation création\par Controller -> UI: Redirect avec succès\par UI -> Utilisateur: Message de confirmation\par else Conflits détectés\par Service -> Controller: Liste erreurs\par Controller -> UI: Redirect avec erreurs\par UI -> Utilisateur: Messages d'erreur\par end\par @enduml\par \par \par {\b FIN DE LA CONCEPTION}\par \par \par {\i Ce document de conception constitue la référence technique complète pour le développement, la maintenance et l'évolution du Système de Gestion des Emplois du Temps. Il doit être maintenu à jour selon les évolutions du projet.}\par \par {\b Version} : 1.0\par {\b Date} : Octobre 2024\par {\b Statut} : Document de référence approuvé\par }